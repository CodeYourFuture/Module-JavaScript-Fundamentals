const twelveHourClockTime = "8:53pm";
const twentyFourHourClockTime = "20:53";

console.log(twelveHourClockTime);
console.log(twentyFourHourClockTime);

// Original code used `12HourClockTime` and `24hourClockTime` as variable names.
// but these start with numbers, which JavaScript doesn't allow 
// variable names must start with a letter, underscore, or dollar sign.
// So I renamed them to `twelveHourClockTime` and `twentyFourHourClockTime`.
// This avoids the SyntaxError and makes the code run.
