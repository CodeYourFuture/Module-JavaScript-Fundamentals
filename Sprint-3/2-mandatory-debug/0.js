// Predict and explain first...

// Prediction:I think it will print 320, but the template string might not show the right result, but will return nothing.

function multiply(a, b) {
  return a * b;
}

console.log(`The result of multiplying 10 and 32 is ${multiply(10, 32)}`);

// because console.log will print whatever it is given . 
// the function has no return statement, so when we run the code it will say undefined because there is nothing to give back.

// Finally, correct the code to fix the problem
//  =============> write your new code here
